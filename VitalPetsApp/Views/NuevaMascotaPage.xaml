<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="VitalPetsApp.Views.NuevaMascotaPage"
             Title="Registrar Mascota">

    <ScrollView>
        <StackLayout Padding="10" Spacing="10">
            <Entry x:Name="NombreEntry" Placeholder="Nombre" />
            <Entry x:Name="EspecieEntry" Placeholder="Especie" />
            <Entry x:Name="RazaEntry" Placeholder="Raza" />
            <DatePicker x:Name="FechaNacimientoPicker" />

            <Entry x:Name="UsuarioIdEntry" Placeholder="ID del Dueño (UsuarioId)" Keyboard="Numeric" />

            <Button Text="Guardar"
                    BackgroundColor="#2196F3"
                    TextColor="White"
                    Clicked="OnGuardarClicked"/>
        </StackLayout>
    </ScrollView>
</ContentPage>
